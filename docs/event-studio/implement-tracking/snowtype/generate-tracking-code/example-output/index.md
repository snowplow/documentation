---
title: "Example outputs"
sidebar_label: "Example outputs"
sidebar_position: 1
description: "Example configuration files and generated output for each Snowtype-supported tracker and language."
keywords: ["Snowtype", "code generation", "output", "example", "tracker"]
date: "2026-03-19"
---

The following examples show the Snowtype configuration file and generated output for the Iglu Central schema `web_page`, and a custom data structure, `product`. The `tracker` and `language` fields in your configuration determine the language and structure of the output.

TODO check again against Snowtype code

## Browser tracker

TODO

### TypeScript

Use this example if you are generating code for the `@snowplow/browser-tracker` in TypeScript. TODO etc

```json title="snowtype.config.json"
{
  "orgId": "your-org-id",
  "tracker": "@snowplow/browser-tracker",
  "language": "typescript",
  "outpath": "./src/tracking/snowplow",
  "igluCentralSchemas": [
    "iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0"
  ],
  "dataStructures": [
    "iglu:com.example/product/jsonschema/1-0-0"
  ]
}
```

```tsx title="Generated output"
import {
  trackSelfDescribingEvent,
  CommonEventProperties,
  SelfDescribingJson,
} from "@snowplow/browser-tracker";
// Automatically generated by Snowtype

/**
 * Schema for a web page
 */
export type WebPage = {
  /** An identifier for the web page. */
  id: string;
};

/**
 * product
 */
export type Product = {
  /**
   * The product ID.
   */
  id: string;
  /**
   * The product name.
   */
  name: string;
  /**
   * The currency the product is listed in.
   */
  currency: string;
  /**
   * The price of the product.
   */
  price: number;
  /**
   * The product category.
   */
  category: string;
};

type ContextsOrTimestamp<T = any> = Omit<
  CommonEventProperties<T>,
  "context"
> & { context?: SelfDescribingJson<T>[] | null | undefined };

/**
 * Track a Snowplow event for WebPage.
 * Schema for a web page
 */
export function trackWebPage<T extends {} = any>(
  webPage: WebPage & ContextsOrTimestamp<T>,
  trackers?: string[]
) {
  const { context, timestamp, ...data } = webPage;
  const event: SelfDescribingJson = {
    schema: 'iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0',
    data
  };

  trackSelfDescribingEvent({
    event,
    context,
    timestamp,
  }, trackers);
}

/**
 * Creates a Snowplow WebPage entity.
 */
export function createWebPage(webPage: WebPage) {
  return {
    schema: 'iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0',
    data: webPage
  }
}

/**
 * Track a Snowplow event for Product.
 * product
 */
export function trackProduct<T extends {} = any>(
  product: Product & ContextsOrTimestamp<T>,
  trackers?: string[]
) {
  const { context, timestamp, ...data } = product;
  const event: SelfDescribingJson = {
    schema: 'iglu:com.example/product/jsonschema/1-0-0',
    data
  };

  trackSelfDescribingEvent({
    event,
    context,
    timestamp,
  }, trackers);
}

/**
 * Creates a Snowplow Product entity.
 */
export function createProduct(product: Product) {
  return {
    schema: 'iglu:com.example/product/jsonschema/1-0-0',
    data: product
  }
}
```

---
### JavaScript

Use this example if you are generating code for the `@snowplow/browser-tracker` in JavaScript. The configuration and output show how to track a web page and a product entity using plain JavaScript.

```json title="snowtype.config.json"
{
  "orgId": "your-org-id",
  "tracker": "@snowplow/browser-tracker",
  "language": "javascript",
  "outpath": "./src/tracking/snowplow",
  "igluCentralSchemas": [
    "iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0"
  ],
  "dataStructures": [
    "iglu:com.example/product/jsonschema/1-0-0"
  ]
}
```

```js title="Generated output"
import {
  trackSelfDescribingEvent,
} from "@snowplow/browser-tracker";

/**
 * Track a Snowplow event for WebPage.
 * Schema for a web page
 * @param { WebPage & Timestamp & Context } webPage - Attributes for WebPage.
 * @param {string[]} [trackers] - Tracker names to send the event to.
 */
export function trackWebPage(webPage, trackers) {
  const { context, timestamp, ...data } = webPage;
  const event = {
    schema: 'iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0',
    data
  };

  trackSelfDescribingEvent({
    event,
    context,
    timestamp,
  }, trackers);
}

/**
 * Creates a Snowplow WebPage entity.
 * @param { WebPage } webPage - Attributes for WebPage.
 */
export function createWebPage(webPage) {
  return {
    schema: 'iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0',
    data: webPage
  }
}

/**
 * Track a Snowplow event for Product.
 * product
 * @param { Product & Timestamp & Context } product - Attributes for Product.
 * @param {string[]} [trackers] - Tracker names to send the event to.
 */
export function trackProduct(product, trackers) {
  const { context, timestamp, ...data } = product;
  const event = {
    schema: 'iglu:com.example/product/jsonschema/1-0-0',
    data
  };

  trackSelfDescribingEvent({
    event,
    context,
    timestamp,
  }, trackers);
}

/**
 * Creates a Snowplow Product entity.
 * @param { Product } product - Attributes for Product.
 */
export function createProduct(product) {
  return {
    schema: 'iglu:com.example/product/jsonschema/1-0-0',
    data: product
  }
}
```

---
## JavaScript (tag) tracker

Use this example if you are generating code for the browser tag web tracker in JavaScript.

```json title="snowtype.config.json"
{
  "orgId": "your-org-id",
  "tracker": "@snowplow/javascript-tracker",
  "language": "javascript",
  "outpath": "./src/tracking/snowplow",
  "igluCentralSchemas": [
    "iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0"
  ],
  "dataStructures": [
    "iglu:com.example/product/jsonschema/1-0-0"
  ]
}
```

```js title="Generated output"
/**
 * Track a Snowplow event for WebPage.
 * Schema for a web page
 * @param { WebPage & Timestamp & Context } webPage - Attributes for WebPage.
 * @param {string[]} [trackers] - Tracker names to send the event to.
 */
function trackWebPage(webPage, trackers) {
  const trackerNames = (trackers && trackers.length) ? ':' + trackers.join(';') : '';
  const { context, timestamp, ...data } = webPage;
  window.snowplow('trackSelfDescribingEvent' + trackerNames, {
    event: {
      schema: 'iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0',
      data
    },
    context,
    timestamp,
  });
}

/**
 * Creates a Snowplow WebPage entity.
 * @param { WebPage } webPage - Attributes for WebPage.
 */
function createWebPage(webPage) {
  return {
    schema: 'iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0',
    data: webPage
  }
}

/**
 * Track a Snowplow event for Product.
 * product
 * @param { Product & Timestamp & Context } product - Attributes for Product.
 * @param {string[]} [trackers] - Tracker names to send the event to.
 */
function trackProduct(product, trackers) {
  const trackerNames = (trackers && trackers.length) ? ':' + trackers.join(';') : '';
  const { context, timestamp, ...data } = product;
  window.snowplow('trackSelfDescribingEvent' + trackerNames, {
    event: {
      schema: 'iglu:com.example/product/jsonschema/1-0-0',
      data
    },
    context,
    timestamp,
  });
}

/**
 * Creates a Snowplow Product entity.
 * @param { Product } product - Attributes for Product.
 */
function createProduct(product) {
  return {
    schema: 'iglu:com.example/product/jsonschema/1-0-0',
    data: product
  }
}
```

---
## iOS tracker

Use this example if you are generating code for the `snowplow-ios-tracker` in Swift.

```json title="snowtype.config.json"
{
  "orgId": "your-org-id",
  "tracker": "snowplow-ios-tracker",
  "language": "swift",
  "outpath": "./Sources/Tracking/Snowplow",
  "igluCentralSchemas": [
    "iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0"
  ],
  "dataStructures": [
    "iglu:com.example/product/jsonschema/1-0-0"
  ]
}
```

```swift title="Generated output"
import Foundation
import SnowplowTracker
// Automatically generated by Snowtype

/// Schema for a web page
struct WebPage {
    /// An identifier for the web page.
    var id: String

    private var schema: String {
        return "iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0"
    }

    private var payload: [String: Any] {
        return ["id": id]
    }

    /// Creates an event instance to be tracked by the tracker.
    func toEvent() -> SelfDescribing {
        return SelfDescribing(schema: schema, payload: payload)
    }

    /// Creates an entity that can be added to events.
    func toEntity() -> SelfDescribingJson {
        return SelfDescribingJson(schema: schema, andData: payload)
    }
}

/// product
struct Product {
    /// The product ID.
    var id: String
    /// The product name.
    var name: String
    /// The currency the product is listed in.
    var currency: String
    /// The price of the product.
    var price: Double
    /// The product category.
    var category: String

    private var schema: String {
        return "iglu:com.example/product/jsonschema/1-0-0"
    }

    private var payload: [String: Any] {
        return [
            "id": id,
            "name": name,
            "currency": currency,
            "price": price,
            "category": category,
        ]
    }

    func toEvent() -> SelfDescribing {
        return SelfDescribing(schema: schema, payload: payload)
    }

    func toEntity() -> SelfDescribingJson {
        return SelfDescribingJson(schema: schema, andData: payload)
    }
}
```

---
## Android

Use this example if you are generating code for the `snowplow-android-tracker` in Kotlin.

```json title="snowtype.config.json"
{
  "orgId": "your-org-id",
  "tracker": "snowplow-android-tracker",
  "language": "kotlin",
  "outpath": "./src/main/java/com/example/tracking",
  "igluCentralSchemas": [
    "iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0"
  ],
  "dataStructures": [
    "iglu:com.example/product/jsonschema/1-0-0"
  ]
}
```

```kotlin title="Generated output"
import com.snowplowanalytics.snowplow.event.SelfDescribing
import com.snowplowanalytics.snowplow.payload.SelfDescribingJson
// Automatically generated by Snowtype

/**
 * Schema for a web page
 */
data class WebPage(
    /** An identifier for the web page. */
    var id: String
) {
    private val schema = "iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0"

    private val payload: HashMap<String, Any?>
        get() {
            val payload = HashMap<String, Any?>()
            payload["id"] = id
            return payload
        }

    fun toEvent(): SelfDescribing {
        return SelfDescribing(schema, payload)
    }

    fun toEntity(): SelfDescribingJson {
        return SelfDescribingJson(schema, payload)
    }
}

/**
 * product
 */
data class Product(
    /** The product ID. */
    var id: String,
    /** The product name. */
    var name: String,
    /** The currency the product is listed in. */
    var currency: String,
    /** The price of the product. */
    var price: Double,
    /** The product category. */
    var category: String
) {
    private val schema = "iglu:com.example/product/jsonschema/1-0-0"

    private val payload: HashMap<String, Any?>
        get() {
            val payload = HashMap<String, Any?>()
            payload["id"] = id
            payload["name"] = name
            payload["currency"] = currency
            payload["price"] = price
            payload["category"] = category
            return payload
        }

    fun toEvent(): SelfDescribing {
        return SelfDescribing(schema, payload)
    }

    fun toEntity(): SelfDescribingJson {
        return SelfDescribingJson(schema, payload)
    }
}
```

---
## React Native (TypeScript)

Use this example if you are generating code for the `@snowplow/react-native-tracker` in TypeScript. The configuration and output show how to track events and create entities in a React Native app.

```json title="snowtype.config.json"
{
  "orgId": "your-org-id",
  "tracker": "@snowplow/react-native-tracker",
  "language": "typescript",
  "outpath": "./src/tracking/snowplow",
  "igluCentralSchemas": [
    "iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0"
  ],
  "dataStructures": [
    "iglu:com.example/product/jsonschema/1-0-0"
  ]
}
```

```tsx title="Generated output"
// Automatically generated by Snowtype

interface CommonEventProperties<T = Record<string, unknown>> {
  /** Add context to an event by setting an Array of Self Describing JSON */
  contexts?: Array<SelfDescribing<T>>;
}

type Contexts<T = any> = Omit<CommonEventProperties<T>, 'context'> & { context?: SelfDescribing<T>[] }

/**
 * SelfDescribing type
 */
export type SelfDescribing<T extends Record<keyof T, unknown> = Record<string, unknown>> = {
    /**
     * The schema string
     * @example 'iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0'
     */
    schema: string;
    /**
     * The data object which should conform to the supplied schema
     */
    data: T;
};

export type WebPage = {
  /** An identifier for the web page. */
  id: string;
};

/**
 * product
 */
export type Product = {
  /**
   * The product ID.
   */
  id: string;
  /**
   * The product name.
   */
  name: string;
  /**
   * The currency the product is listed in.
   */
  currency: string;
  /**
   * The price of the product.
   */
  price: number;
  /**
   * The product category.
   */
  category: string;
};

/**
 * Track a Snowplow event for WebPage.
 * Schema for a web page
 */
export function trackWebPage<T extends {} = any>(tracker: ReactNativeTracker, webPage: WebPage & Contexts<T>) {
  const { contexts, ...data } = webPage;
  tracker.trackSelfDescribingEvent({
    schema: 'iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0',
    data
  }, contexts);
}

/**
 * Creates a Snowplow WebPage entity.
 */
export function createWebPage(webPage: WebPage) {
  return {
    schema: 'iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0',
    data: webPage
  }
}

/**
 * Track a Snowplow event for Product.
 * product
 */
export function trackProduct<T extends {} = any>(tracker: ReactNativeTracker, product: Product & Contexts<T>) {
  const { contexts, ...data } = product;
  tracker.trackSelfDescribingEvent({
    schema: 'iglu:com.example/product/jsonschema/1-0-0',
    data
  }, contexts);
}

/**
 * Creates a Snowplow Product entity.
 */
export function createProduct(product: Product) {
  return {
    schema: 'iglu:com.example/product/jsonschema/1-0-0',
    data: product
  }
}
```

---
## Flutter (Dart)

Use this example if you are generating code for the `snowplow-flutter-tracker` in Dart. The configuration and output show how to define immutable classes for tracking events in a Flutter app.

```json title="snowtype.config.json"
{
  "orgId": "your-org-id",
  "tracker": "snowplow-flutter-tracker",
  "language": "dart",
  "outpath": "./lib/tracking/snowplow",
  "igluCentralSchemas": [
    "iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0"
  ],
  "dataStructures": [
    "iglu:com.example/product/jsonschema/1-0-0"
  ]
}
```

```dart title="Generated output"
import 'package:flutter/foundation.dart';
import 'package:snowplow_tracker/events/self_describing.dart';
// Automatically generated by Snowtype

/// Schema for a web page
/// Schema: `iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0`
@immutable
class WebPage implements SelfDescribing {
  /// An identifier for the web page.
  final String id;

  const WebPage({required this.id});

  @override
  String endpoint() => 'trackSelfDescribing';

  @override
  Map<String, Object?> get data => {'id': id};

  @override
  String get schema =>
      'iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0';

  @override
  Map<String, Object?> toMap() => {'schema': schema, 'data': data};
}

/// product
/// Schema: `iglu:com.example/product/jsonschema/1-0-0`
@immutable
class Product implements SelfDescribing {
  /// The product ID.
  final String id;
  /// The product name.
  final String name;
  /// The currency the product is listed in.
  final String currency;
  /// The price of the product.
  final double price;
  /// The product category.
  final String category;

  const Product({
    required this.id,
    required this.name,
    required this.currency,
    required this.price,
    required this.category,
  });

  @override
  String endpoint() => 'trackSelfDescribing';

  @override
  Map<String, Object?> get data => {
    'id': id,
    'name': name,
    'currency': currency,
    'price': price,
    'category': category,
  };

  @override
  String get schema => 'iglu:com.example/product/jsonschema/1-0-0';

  @override
  Map<String, Object?> toMap() => {'schema': schema, 'data': data};
}
```

---
## Node.js

### TypeScript

Use this example if you are generating code for the `@snowplow/node-tracker` in TypeScript. The configuration and output show how to track events and create entities in a Node.js application.

```json title="snowtype.config.json"
{
  "orgId": "your-org-id",
  "tracker": "@snowplow/node-tracker",
  "language": "typescript",
  "outpath": "./src/tracking/snowplow",
  "igluCentralSchemas": [
    "iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0"
  ],
  "dataStructures": [
    "iglu:com.example/product/jsonschema/1-0-0"
  ]
}
```

```tsx title="Generated output"
import {
  buildSelfDescribingEvent,
  CommonEventProperties,
  SelfDescribingJson,
  Tracker,
} from "@snowplow/node-tracker";

export type WebPage = {
  id: string;
};

/**
 * product
 */
export type Product = {
  /**
   * The product ID.
   */
  id: string;
  /**
   * The product name.
   */
  name: string;
  /**
   * The currency the product is listed in.
   */
  currency: string;
  /**
   * The price of the product.
   */
  price: number;
  /**
   * The product category.
   */
  category: string;
};

type ContextsOrTimestamp<T = any> = Omit<
  CommonEventProperties<T>,
  "context"
> & { context?: SelfDescribingJson<T>[] | null | undefined };

/**
 * Track a Snowplow event for WebPage.
 * Schema for a web page
 */
export function trackWebPage<T extends {} = any>(tracker: Tracker, webPage: WebPage & ContextsOrTimestamp<T>) {
  const { context, timestamp, ...data } = webPage;
  tracker.track(buildSelfDescribingEvent({
    event: {
      schema: 'iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0',
      data
    }
  }), context, timestamp);
}

/**
 * Creates a Snowplow WebPage entity.
 */
export function createWebPage(webPage: WebPage) {
  return {
    schema: 'iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0',
    data: webPage
  }
}

/**
 * Track a Snowplow event for Product.
 * product
 */
export function trackProduct<T extends {} = any>(tracker: Tracker, product: Product & ContextsOrTimestamp<T>) {
  const { context, timestamp, ...data } = product;
  tracker.track(buildSelfDescribingEvent({
    event: {
      schema: 'iglu:com.example/product/jsonschema/1-0-0',
      data
    }
  }), context, timestamp);
}

/**
 * Creates a Snowplow Product entity.
 */
export function createProduct(product: Product) {
  return {
    schema: 'iglu:com.example/product/jsonschema/1-0-0',
    data: product
  }
}
```

---
### JavaScript

Use this example if you are generating code for the `@snowplow/node-tracker` in JavaScript. The configuration and output show how to track events and create entities in a Node.js application using plain JavaScript.

```json title="snowtype.config.json"
{
  "orgId": "your-org-id",
  "tracker": "@snowplow/node-tracker",
  "language": "javascript",
  "outpath": "./src/tracking/snowplow",
  "igluCentralSchemas": [
    "iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0"
  ],
  "dataStructures": [
    "iglu:com.example/product/jsonschema/1-0-0"
  ]
}
```

```js title="Generated output"
import { buildSelfDescribingEvent } from "@snowplow/node-tracker";
// Automatically generated by Snowtype

/**
 * Track a Snowplow event for WebPage.
 * Schema for a web page
 * @param {Tracker} tracker - Tracker to send the event to.
 * @param { WebPage & Timestamp & Context } webPage - Attributes for WebPage.
 */
export function trackWebPage(tracker, webPage) {
  const { context, timestamp, ...data } = webPage;

  tracker.track(buildSelfDescribingEvent({
    event: {
      schema: 'iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0',
      data
    }
  }), context, timestamp);
}

/**
 * Creates a Snowplow WebPage entity.
 * @param { WebPage } webPage - Attributes for WebPage.
 */
export function createWebPage(webPage) {
  return {
    schema: 'iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0',
    data: webPage
  }
}

/**
 * Track a Snowplow event for Product.
 * product
 * @param {Tracker} tracker - Tracker to send the event to.
 * @param { Product & Timestamp & Context } product - Attributes for Product.
 */
export function trackProduct(tracker, product) {
  const { context, timestamp, ...data } = product;

  tracker.track(buildSelfDescribingEvent({
    event: {
      schema: 'iglu:com.example/product/jsonschema/1-0-0',
      data
    }
  }), context, timestamp);
}

/**
 * Creates a Snowplow Product entity.
 * @param { Product } product - Attributes for Product.
 */
export function createProduct(product) {
  return {
    schema: 'iglu:com.example/product/jsonschema/1-0-0',
    data: product
  }
}
```

---
## Go

Use this example if you are generating code for the `snowplow-golang-tracker` in Go. The configuration and output show how to define structs and track events in a Go application.

```json title="snowtype.config.json"
{
  "orgId": "your-org-id",
  "tracker": "snowplow-golang-tracker",
  "language": "go",
  "outpath": "./tracking/snowplow",
  "igluCentralSchemas": [
    "iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0"
  ],
  "dataStructures": [
    "iglu:com.example/product/jsonschema/1-0-0"
  ]
}
```

```go title="Generated output"
package snowtype
// Automatically generated by Snowtype

import (
	sp "github.com/snowplow/snowplow-golang-tracker/v3/tracker"
	"github.com/mitchellh/mapstructure"
)

// Schema for a web page
type WebPage struct {
	// An identifier for the web page.
	ID string `mapstructure:"id"`
}

func (webPage WebPage) SnowplowFormat() (*sp.SelfDescribingJson, error) {
	var decodedMap map[string]interface{}
	err := mapstructure.Decode(webPage, &decodedMap)
	if err != nil {
		return nil, err
	}
	sdj := sp.InitSelfDescribingJson(
		"iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0",
		decodedMap,
	)
	return sdj, nil
}

func TrackWebPage(tracker *sp.Tracker, webPage WebPage, opts ...option) error {
	sdj, err := webPage.SnowplowFormat()
	if err != nil {
		return err
	}
	sde := &sp.SelfDescribingEvent{Event: sdj}
	for _, opt := range opts {
		opt(sde)
	}
	tracker.TrackSelfDescribingEvent(*sde)
	return nil
}

// product
type Product struct {
	// The product ID.
	ID       string  `mapstructure:"id"`
	// The product name.
	Name     string  `mapstructure:"name"`
	// The currency the product is listed in.
	Currency string  `mapstructure:"currency"`
	// The price of the product.
	Price    float64 `mapstructure:"price"`
	// The product category.
	Category string  `mapstructure:"category"`
}

func (product Product) SnowplowFormat() (*sp.SelfDescribingJson, error) {
	var decodedMap map[string]interface{}
	err := mapstructure.Decode(product, &decodedMap)
	if err != nil {
		return nil, err
	}
	sdj := sp.InitSelfDescribingJson(
		"iglu:com.example/product/jsonschema/1-0-0",
		decodedMap,
	)
	return sdj, nil
}

func TrackProduct(tracker *sp.Tracker, product Product, opts ...option) error {
	sdj, err := product.SnowplowFormat()
	if err != nil {
		return err
	}
	sde := &sp.SelfDescribingEvent{Event: sdj}
	for _, opt := range opts {
		opt(sde)
	}
	tracker.TrackSelfDescribingEvent(*sde)
	return nil
}
```

---
## Java

Use this example if you are generating code for the `snowplow-java-tracker` in Java. The configuration and output show how to define classes and track events in a Java application.

```json title="snowtype.config.json"
{
  "orgId": "your-org-id",
  "tracker": "snowplow-java-tracker",
  "language": "java",
  "outpath": "./src/main/java/com/snowplowanalytics/snowplow/snowtype",
  "igluCentralSchemas": [
    "iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0"
  ],
  "dataStructures": [
    "iglu:com.example/product/jsonschema/1-0-0"
  ]
}
```

```java title="Generated output (WebPage.java)"
// Automatically generated by Snowtype
package com.snowplowanalytics.snowplow.snowtype;

import com.snowplowanalytics.snowplow.tracker.payload.SelfDescribingJson;
import java.util.*;

/**
 * Schema for a web page
 */
public class WebPage {
    private static final String SCHEMA =
        "iglu:com.snowplowanalytics.snowplow/web_page/jsonschema/1-0-0";

    private final String id;

    private WebPage(Builder builder) {
        this.id = Objects.requireNonNull(builder.id, "id is required");
    }

    private Map<String, Object> getPayload() {
        Map<String, Object> payload = new HashMap<>();
        payload.put("id", id);
        return payload;
    }

    public SelfDescribingJson toSelfDescribingJson() {
        return new SelfDescribingJson(SCHEMA, getPayload());
    }

    public static class Builder {
        private String id;

        public Builder setId(String id) {
            this.id = id;
            return this;
        }

        public WebPage build() {
            return new WebPage(this);
        }
    }
}
```

```java title="Generated output (Product.java)"
// Automatically generated by Snowtype
package com.snowplowanalytics.snowplow.snowtype;

import com.snowplowanalytics.snowplow.tracker.payload.SelfDescribingJson;
import java.util.*;

/**
 * product
 */
public class Product {
    private static final String SCHEMA =
        "iglu:com.example/product/jsonschema/1-0-0";

    private final String id;
    private final String name;
    private final String currency;
    private final Double price;
    private final String category;

    private Product(Builder builder) {
        this.id = Objects.requireNonNull(builder.id, "id is required");
        this.name = Objects.requireNonNull(builder.name, "name is required");
        this.currency = Objects.requireNonNull(builder.currency, "currency is required");
        this.price = Objects.requireNonNull(builder.price, "price is required");
        this.category = Objects.requireNonNull(builder.category, "category is required");
    }

    private Map<String, Object> getPayload() {
        Map<String, Object> payload = new HashMap<>();
        payload.put("id", id);
        payload.put("name", name);
        payload.put("currency", currency);
        payload.put("price", price);
        payload.put("category", category);
        return payload;
    }

    public SelfDescribingJson toSelfDescribingJson() {
        return new SelfDescribingJson(SCHEMA, getPayload());
    }

    public static class Builder {
        private String id;
        private String name;
        private String currency;
        private Double price;
        private String category;

        public Builder setId(String id) { this.id = id; return this; }
        public Builder setName(String name) { this.name = name; return this; }
        public Builder setCurrency(String currency) { this.currency = currency; return this; }
        public Builder setPrice(Double price) { this.price = price; return this; }
        public Builder setCategory(String category) { this.category = category; return this; }

        public Product build() {
            return new Product(this);
        }
    }
}
```
